import 'package:flutter/material.dart';

class ExpenseTypeScreen extends StatelessWidget {
  const ExpenseTypeScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Expense Type Screen'));
}
