import 'package:flutter/material.dart';

class ExpansesTypeScreen extends StatelessWidget {
  const ExpansesTypeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Expanses Type'),
      ),
    );
  }
}
